import 'package:flutter/material.dart';

class AppColors {
  static const Color _seedColor = Colors.deepOrangeAccent;
  static final ColorScheme colorScheme = ColorScheme.fromSeed(seedColor: _seedColor);
}